# Backend Environment Variables Example

# Node Environment
NODE_ENV=development

# Frontend URL for CORS
# For local development
FRONTEND_URL=http://localhost:3000

# For production (this will be overridden by GitHub Actions)
# FRONTEND_URL=https://fgo-servant-quiz.so-ta.net

# Server Port (will be overridden by Cloud Run's PORT variable in production)
PORT=8888
